<div class="cube">
    <img class="cube" src={{imageUrl}}
        alt="cube-image">
    <p class="name">{{name}}</p>
    <p><span>Difficulty level:</span>
        {{difficulty}}
    </p>
    <a class="btn" href="/details/{{_id}}">Details</a>
</div>